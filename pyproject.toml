[project]
name = "scan-assess-gui"
version = "0.1.0"
description = "NiceGUI workbench for scan-assess reports, validation, module controls, and LLM configuration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "httpx>=0.27",
    "nicegui>=2.0",
    "openai>=2.36.0",
    "pydantic>=2.7",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
    "rich>=13.7",
    "typer>=0.12",
]
